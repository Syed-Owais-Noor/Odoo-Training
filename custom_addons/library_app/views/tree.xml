<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="library_book_view_tree" model="ir.ui.view">
        <field name="name">library.book.view.tree</field>
        <field name="model">library.book</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name" />
                <field name="publisher_id" string="Pusblisher" />
                <field name="auther_ids" string="Authors" />
                <field name="date_published" string="Authors" />
                <field name="isbn" string="Authors" />
            </tree>
        </field>
    </record>

</odoo>
